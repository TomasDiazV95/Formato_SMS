portal.html

<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <title>Panel Principal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="{{ url_for('static', filename='style.css') }}" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="card shadow-sm border-0 rounded-4 p-4">
      <h1 class="h3 mb-3">Centro de Operaciones</h1>
      <p class="text-muted">Selecciona la sección que necesitas.</p>
      <div class="d-grid gap-3">
        <a class="btn btn-primary btn-lg" href="{{ url_for('sms.procesos_menu') }}">⚙️ Procesos</a>
        <a class="btn btn-primary btn-lg" href="{{ url_for('sms.cargas_menu') }}">📦 Cargas</a>
        <a class="btn btn-primary btn-lg" href="{{ url_for('resultantes.resultantes_page') }}">🧾 Resultantes</a>
        <a class="btn btn-secondary btn-lg" href="{{ url_for('reports.reports_page') }}">📊 Reportes</a>
      </div>
    </div>
  </div>
</body>
</html>


<---------------------------------------------------------------------------------------------->




resultante_tanner.html



<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Resultante Tanner</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="{{ url_for('static', filename='style.css') }}" rel="stylesheet">
</head>
<body class="bg-soft">
  <a href="{{ url_for('sms.main') }}" class="btn btn-primary btn-sm position-fixed top-0 end-0 m-3 shadow">🏠 Inicio</a>

  <div class="container py-5">
    <div class="mx-auto" style="max-width: 950px;">
      {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
          <div class="mb-4">
            {% for category, msg in messages %}
              <div class="alert alert-{{category}} rounded-3" role="alert">{{ msg }}</div>
            {% endfor %}
          </div>
        {% endif %}
      {% endwith %}

      <div class="card shadow-sm border-0 rounded-4 overflow-hidden mb-4">
        <div class="p-4 p-md-5 header-grad text-black">
          <h1 class="h4 mb-1">Resultante Tanner</h1>
          <p class="mb-0 opacity-75">Selecciona fecha de inicio y término para generar la consulta basada en QUERY.txt.</p>
        </div>

        <div class="p-4 p-md-5 bg-white">
          <form method="POST" class="row g-3 needs-validation" novalidate>
            <div class="col-md-4">
              <label class="form-label fw-semibold">Fecha inicio</label>
              <input class="form-control" type="date" name="fecha_inicio" value="{{ fecha_inicio }}" required>
              <div class="invalid-feedback">Debes ingresar la fecha de inicio.</div>
            </div>

            <div class="col-md-4">
              <label class="form-label fw-semibold">Fecha término</label>
              <input class="form-control" type="date" name="fecha_fin" value="{{ fecha_fin }}" required>
              <div class="invalid-feedback">Debes ingresar la fecha de término.</div>
            </div>

            <div class="col-md-4 d-flex align-items-end gap-2">
              <button class="btn btn-success w-100" type="submit">Generar consulta</button>
              <button
                class="btn btn-outline-primary w-100"
                type="submit"
                formaction="{{ url_for('resultantes.download_resultante_tanner_query') }}"
                formmethod="post"
              >
                Descargar .txt
              </button>
            </div>
          </form>

          {% if query_generada %}
            <hr class="my-4">
            {% if total_registros is not none %}
              <div class="alert alert-info rounded-3" role="alert">
                Registros obtenidos: <b>{{ total_registros }}</b>
              </div>
            {% endif %}
            <label class="form-label fw-semibold">Consulta generada</label>
            <textarea class="form-control" rows="18" readonly>{{ query_generada }}</textarea>
            <div class="form-text mt-2">La descarga ejecuta la consulta y exporta resultados en <b>.txt</b> separados por <b>|</b>.</div>
          {% endif %}
        </div>
      </div>

      <a href="{{ url_for('resultantes.resultantes_page') }}" class="btn btn-outline-secondary">← Volver a Resultantes</a>
    </div>
  </div>

  <script>
    (() => {
      'use strict'
      const forms = document.querySelectorAll('.needs-validation')
      Array.from(forms).forEach(form => {
        form.addEventListener('submit', event => {
          if (!form.checkValidity()) {
            event.preventDefault()
            event.stopPropagation()
          }
          form.classList.add('was-validated')
        }, false)
      })
    })()
  </script>
</body>
</html>







<---------------------------------------------------------------------------------------------->



resultantes.html






<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Resultantes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="{{ url_for('static', filename='style.css') }}" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <a href="{{ url_for('sms.main') }}" class="btn btn-outline-secondary btn-sm mb-3">← Volver</a>
    <div class="card shadow-sm border-0 rounded-4 p-4">
      <h1 class="h4 mb-3">Resultantes</h1>
      <p class="text-muted mb-4">Selecciona la resultante que quieres generar.</p>

      <div class="d-grid gap-3">
        <a class="btn btn-primary btn-lg" href="{{ url_for('resultantes.resultante_tanner_page') }}">Resultante Tanner</a>
      </div>
    </div>
  </div>
</body>
</html>
